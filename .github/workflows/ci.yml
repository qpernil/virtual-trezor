name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  integration-boundary:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Initialize pinned Trezor One dependencies
        run: make init
      - name: Verify upstream and platform boundary
        run: make check
